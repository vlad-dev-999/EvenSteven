Continue from the previous implementation. Do not rediscover the architecture or redesign anything.

Current state

- Appearance Engine already exists.
- Themes are CSS-token based.
- Dynamic theme switching is implemented.
- Preserve the existing architecture.
- Apply the smallest possible production-safe changes.

Complete the remaining work:

1. Ambience Packs
- Build on the existing Appearance Engine.
- Implement premium ambience using CSS only where possible.
- Rain: faint droplets on glass.
- Storm: slow rolling fog.
- Ocean: gentle caustic light.
- Christmas: sparse slow snowfall.
- Night Sky: distant stars.
- Keep all effects extremely subtle, GPU-friendly, behind the UI, responsive, and respect prefers-reduced-motion.

2. Member Identity
- Replace free-text crest entry with a curated Unicode/Emoji picker.
- Do not use uploaded images.
- If persistent member colours require a database migration, STOP and ask for approval before modifying the schema or running Drizzle.
- Do not perform a Drizzle push without explicit approval.
- If a schema change is unavoidable, explain exactly why it is required and what files will change before making any edits.

3. Appearance Settings
- Add a "Subtle Motion" toggle.
- Motion should only affect premium micro-interactions (hover, ripple, card breathing).
- No sounds.

4. Dynamic Evening Highlight
- Reuse existing balances/event data.
- Do not introduce additional API calls.
- Display the configured Unicode highlight icon beside the current highest contributor.
- No labels.
- No tooltips.
- No notifications.
- No animations when the icon changes.
- If there is a tie, display no icon.
- This is a living indicator, not a reward system.

Implementation Rules

- Do not modify business logic.
- Do not redesign components.
- Do not change layouts or responsive behaviour.
- Do not manually edit generated API client files.
- Regenerate generated types if required.
- Do not introduce unnecessary dependencies.
- Build on the existing Appearance Engine only.
- Prefer CSS variables, pseudo-elements and semantic tokens over JavaScript.
- Keep changes modular and minimal.

Quality Gates

After each major stage:
- Build the project.
- Run the TypeScript check.
- Fix any errors before continuing.
- Verify mobile layouts have not regressed.

When complete, provide:
- Files modified.
- Features completed.
- Any remaining work.
- Confirmation that the project builds cleanly and remains production-ready.